# density

Compute solute and total box volumes from `salt` outputs using `cpptraj`.

## Command

```bash
mlip density configs/<molecule>/density/density.yaml
```

## Outputs

- `cpptraj.in`, `cpptraj.out`
- `solute_volume.dat`, `box_volume.dat`
